# Environment Configuration
# WARNING: This file contains sensitive configuration
# DO NOT commit this file to version control

# Application Environment
NODE_ENV=production

# Logging Level (error, warn, info, debug)
LOG_LEVEL=info

# Development Credentials (Set to 'true' to enable fallback credentials)
# WARNING: Only enable this for development/testing purposes
# For production, set this to 'false'
ENABLE_DEV_CREDENTIALS=true

# Default Client Users (JSON array format - STORED IN DATABASE)
# These users are seeded into the database on first run
# SECURITY: Change these passwords before deployment!
DEFAULT_CLIENT_USERS=[{"username":"admin","password":"admin123"}]

# Default Developer Users (JSON array format - NOT STORED IN DATABASE)
# These users can login but are NOT saved to database (for developers only)
# SECURITY: These are for development convenience only, remove in production
DEFAULT_DEV_USERS=[{"username":"pixelVedaAdmin","password":"PixelVeda@2026"},{"username":"pixelVedaTesting","password":"PixelVeda@testing"}]

# Security Settings (Optional - defaults are used if not set)
# BCRYPT_ROUNDS=10
# SESSION_TIMEOUT=1800000

# Database Settings (Optional)
# DB_BACKUP_INTERVAL=86400000
# MAX_BACKUPS=7

# Rate Limiting (Optional)
# RATE_LIMIT_WINDOW=900000
# RATE_LIMIT_MAX_REQUESTS=100
